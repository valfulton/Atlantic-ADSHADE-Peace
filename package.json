{
    "private": true,
    "scripts": {
        "dev": "next dev",
        "build": "next build",
        "kill:docker": "sudo fuser -k 3000/tcp",
        "build:docker": "docker build --no-cache --target prod -t my-nextjs-app:latest .",
        "build:docker:dev": "docker build --no-cache --target dev -t my-nextjs-app:latest .",
        "run:docker": "docker run --rm -p 3000:3000 my-nextjs-app:latest",
        "run:tappd": "docker run --rm -p 8090:8090 phalanetwork/tappd-simulator:latest"
    },
    "dependencies": {
        "@phala/dstack-sdk": "^0.1.7",
        "dotenv": "^16.4.7",
        "file-saver": "^2.0.5",
        "near-api-js": "^5.0.1",
        "near-seed-phrase": "^0.2.1",
        "next": "latest",
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    }
}
